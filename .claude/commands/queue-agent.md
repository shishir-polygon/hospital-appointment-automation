# /queue-agent — Invoke Queue Agent Directly

Invoke the QueueAgent specialist directly.

## Usage
```
/queue-agent <doctor_name_or_department>
```

Read @.claude/agents/queue_agent.md and process `$ARGUMENTS` as a queue status request.
