# /booking-agent — Invoke Booking Agent Directly

Invoke the BookingAgent specialist directly.

## Usage
```
/booking-agent <user_message>
```

Read @.claude/agents/booking_agent.md and process `$ARGUMENTS` as a booking request.
Walk through the booking flow — collect any missing fields, confirm, and finalize.
